spring:
  application:
    name: ai-analyzer-service

  datasource:
    url: jdbc:sqlserver://db57078.public.databaseasp.net:1433;databaseName=db57078;encrypt=true;trustServerCertificate=true
    username: db57078
    password: Bc5-n%Z2Dr3@
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

    hikari:
      read-only: true
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
        jdbc:
          fetch_size: 500
        default_batch_fetch_size: 100

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

server:
  shutdown: graceful
  port: ${PORT:8080}
  address: 0.0.0.0

ai:
  ranking:
    project-id: 0
    result-limit: 20
    embedding-dimension: 300
    recency-period: PER_MONTH
    bug-history-weight: 0.80
    bug-history:
      recency: UNIQUE_BUG_DISTANCE
      half-life-days: 180
      query-expansion: SO_TAG_GRAPH
      expansion-max-tags: 6
      expansion-weight: 0.25
      term-weight: SO_PROJECT_BLEND
      project-weight: 1.00
